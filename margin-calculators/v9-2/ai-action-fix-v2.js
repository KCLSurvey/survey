(()=>{
'use strict';
const frame=document.getElementById('app');
const VERSION='20260715d';
function install(){
 let d;
 try{d=frame.contentDocument}catch{return false}
 if(!d||!d.body)return false;
 if(d.documentElement.dataset.v92AiActionFixV2===VERSION)return true;
 const code=`(()=>{
  const first=(...values)=>values.find(v=>v!==undefined&&v!==null&&!(typeof v==='string'&&v.trim()===''));
  const norm=v=>String(v??'').trim().toLowerCase().replace(/ё/g,'е').replace(/([a-z0-9])([A-Z])/g,'$1_$2').replace(/[^a-zа-я0-9]+/g,'_').replace(/^_+|_+$/g,'');
  const scalar=v=>v&&typeof v==='object'&&!Array.isArray(v)?first(v.id,v.key,v.code,v.name,v.label,v.value):v;
  const fieldKeys=['field','field_name','fieldName','parameter','parameter_name','parameterName','property','key','metric','setting','path'];
  const valueKeys=['value_text','valueText','value','new_value','newValue','new_value_text','newValueText','proposed_value','proposedValue','proposed_value_text','proposedValueText','target_value','targetValue','amount','rate','percentage','percent','pct','to'];
  const containerKeys=['changes','change','fields','updates','update','payload','data','parameters'];
  const inputFields=v91Snapshot().allowedActions.set_input,paramFields=v91Snapshot().allowedActions.set_param;
  const inputAliases={rrp:'rrp',approved_rrp_gross:'rrp',approved_price:'rrp',approvedrrpgross:'rrp',purchase_price:'cogs',purchaseprice:'cogs',cogs:'cogs',vat_rate:'vat',vatrate:'vat',income_tax_rate:'income_tax',incometaxrate:'income_tax',target_value:'single_target_margin',targetvalue:'single_target_margin',target_margin:'single_target_margin',sold_units:'units_total',soldunits:'units_total',purchased_units:'units_total',purchasedunits:'units_total',sku:'single_sku',product_sku:'single_sku'};
  const paramAliases={expense_status:'status',status:'status',expense_name:'name',name:'name',channels:'applies',channel:'applies',sku_mode:'skuMode',skumode:'skuMode',sku_list:'skuList',skulist:'skuList',method:'mode',expense_method:'mode',value:'value',expense_value:'value',rate:'value',percentage:'value',percent:'value',pct:'value',base:'base',category:'treat',expense_category:'treat',vat_treatment:'vatTreatment',vattreatment:'vatTreatment',tax_deductible:'tax',taxdeductible:'tax'};
  const expenseAliases={inbound_logistics:['входящ','логист'],customs_cert:['тамож','сертифик'],warehouse_after_ready:['склад','готов'],warranty_returns:['гарант','сервис'],dtc_lastmile:['последн','мил'],retail_bonus:['ретро','бонус'],trade_marketing:['trade','marketing'],performance_marketing:['performance','media'],sales_team:['команда','продаж'],admin_opex:['административ','opex'],bad_debt:['безнадеж','просроч'],depreciation:['амортизац'],finance_cost:['финансир','процент'],cash_only:['денежн','выплат']};
  function canonicalInput(raw){const n=norm(String(scalar(raw)??'').split('.').pop());const exact=inputFields.find(f=>norm(f)===n);return exact||inputAliases[n]||String(scalar(raw)||'')}
  function canonicalParam(raw){const n=norm(String(scalar(raw)??'').split('.').pop());const exact=paramFields.find(f=>norm(f)===n);return exact||paramAliases[n]||String(scalar(raw)||'')}
  function directField(obj){return first(...fieldKeys.map(k=>obj?.[k]))}
  function directValue(obj){return first(...valueKeys.map(k=>obj?.[k]))}
  function containers(obj){const out=[];for(const k of containerKeys){const v=obj?.[k];if(Array.isArray(v))out.push(...v.filter(x=>x&&typeof x==='object'));else if(v&&typeof v==='object')out.push(v)}return out}
  function findNestedValue(obj){let v=directValue(obj);if(v!==undefined)return v;for(const child of containers(obj)){v=directValue(child);if(v!==undefined)return v}return undefined}
  function resolveChannel(a){const values=[a.target,a.target_id,a.targetId,a.channel,a.channel_id,a.channelId,a.entity,a.entity_id,a.entityId,a.id].map(scalar).filter(v=>v!==undefined);const hint=norm([...values,a.reason,a.description,a.name].filter(Boolean).join(' '));if(/(^|_)retail($|_)|network|сеть|сети|розниц/.test(hint))return 'retail';if(/(^|_)dtc($|_)|direct|прям/.test(hint))return 'dtc';if(/partner|партнер|партнёр/.test(hint))return 'partner';return String(values[0]||'')}
  function resolveParamIndex(a){const candidates=[a.target,a.target_id,a.targetId,a.expense,a.expense_id,a.expenseId,a.entity,a.entity_id,a.entityId,a.id].map(scalar).filter(v=>v!==undefined&&String(v).trim()!=='');for(const raw of candidates){const n=Number(raw);if(Number.isInteger(n)&&params[n])return n;const key=norm(raw);const exact=params.findIndex(p=>norm(p.id)===key||norm(p.name)===key);if(exact>=0)return exact;const partial=params.map((p,i)=>({p,i})).filter(x=>norm(x.p.id).includes(key)||norm(x.p.name).includes(key)||key.includes(norm(x.p.id))||key.includes(norm(x.p.name)));if(partial.length===1)return partial[0].i;const aliases=expenseAliases[key];if(aliases){const matches=params.map((p,i)=>({p,i,n: norm(p.name)})).filter(x=>aliases.every(t=>x.n.includes(norm(t))));if(matches.length===1)return matches[0].i;const loose=params.map((p,i)=>({p,i,n:norm(p.name)})).filter(x=>aliases.some(t=>x.n.includes(norm(t))));if(loose.length===1)return loose[0].i}}
   const context=norm([a.reason,a.description,a.expense_name,a.expenseName,a.name].filter(Boolean).join(' '));if(context){const exact=params.map((p,i)=>({p,i,n:norm(p.name)})).filter(x=>x.n&&context.includes(x.n));if(exact.length===1)return exact[0].i;const scored=params.map((p,i)=>{const tokens=norm(p.name).split('_').filter(t=>t.length>3);return {i,score:tokens.filter(t=>context.includes(t)).length}}).sort((x,y)=>y.score-x.score);if(scored[0]?.score>0&&scored[0].score>scored[1]?.score)return scored[0].i}return -1}
  function mapExpenseField(field,index){const f=canonicalParam(field);if(f!=='value')return f;const mode=String(params[index]?.mode||'');return mode.includes('pct')?'pct':'amount'}
  function normalizeValue(v,field){const s=String(v??'').trim(),n=norm(s);if(field==='status'){if(['include','included','учесть','включить'].includes(n))return 'included';if(['review','проверить'].includes(n))return 'review';if(['na','exclude','excluded','не_применимо','исключить'].includes(n))return 'na'}if(field==='tax'){if(['true','yes','да','вычитается','deductible'].includes(n))return 'yes';if(['false','no','нет','не_вычитается','non_deductible'].includes(n))return 'no'}if(field==='treat'){const map={inventory:'cogs',cogs:'cogs',contra:'contraRevenue',contra_revenue:'contraRevenue',variable:'variableSelling',variable_selling:'variableSelling',marketing:'marketing',fixed:'fixedOpex',fixed_opex:'fixedOpex',depr:'depr',depreciation:'depr',finance:'finance'};return map[n]||s}if(field==='mode'){const map={unit:'unit',pct:'pct',percent:'pct',percentage:'pct',period:'annual',annual:'annual'};return map[n]||s}if(field==='applies'){if(Array.isArray(v))return v.length===1?v[0]:'all';if(n.includes('retail')||n.includes('сет'))return 'retail';if(n.includes('dtc')||n.includes('прям'))return 'dtc';if(n.includes('partner')||n.includes('парт'))return 'partner';return 'all'}return v}
  function expand(action){const a={...(action||{})};let type=norm(first(a.type,a.action,a.action_type,a.actionType));const rawField=directField(a),children=containers(a);if(rawField===undefined){const actionChildren=children.filter(x=>directField(x)!==undefined||containers(x).length);if(actionChildren.length)return actionChildren.flatMap(child=>expand({...a,...child,changes:undefined,change:undefined,fields:undefined,updates:undefined,update:undefined,payload:undefined,data:undefined,parameters:undefined}));const pairs=[];for(const child of children)for(const [k,v] of Object.entries(child))pairs.push([k,v]);if(pairs.length)return pairs.map(([field,value])=>expand({...a,field,value_text:value,changes:undefined,change:undefined,fields:undefined,updates:undefined,update:undefined,payload:undefined,data:undefined,parameters:undefined})[0])}
   const value=findNestedValue(a);
   if(['setchannel','set_channel','update_channel','channel'].includes(type)){const channel=resolveChannel(a),f=norm(rawField);let field='';if(['share','channel_share','share_pct'].includes(f))field='share_'+channel;else if(['sell_in_reduction_pct','sellinreductionpct','sell_in_reduction','commission','commission_pct','channel_commission'].includes(f))field=channel==='retail'?'retail_commission':channel==='partner'?'partner_commission':channel==='dtc'?'dtc_discount':'';else if(['invoice_discount_pct','invoicediscountpct','invoice_discount'].includes(f)&&channel==='dtc')field='dtc_discount';return [{...a,type:'set_input',field,target:'',value_text:value}]}
   if(['setexpense','set_expense','update_expense','expense','setparam','set_param'].includes(type)){const idx=resolveParamIndex(a),field=mapExpenseField(rawField,idx);return [{...a,type:'set_param',target:idx,field,value_text:value}]}
   if(['setproduct','set_product','update_product','product','setglobal','set_global','update_global','global','setcash','set_cash','update_cash','cash'].includes(type)){return [{...a,type:'set_input',field:canonicalInput(rawField),target:'',value_text:value}]}
   const input=canonicalInput(rawField);if(inputFields.includes(input))return [{...a,type:'set_input',field:input,target:'',value_text:value}];return [{...a,type:type||a.type,field:input,value_text:value}]
  }
  function normalizeAll(actions){return (actions||[]).flatMap(expand).filter(a=>a&&a.type&&a.type!=='none')}
  const originalRender=v91RenderActions;
  v91RenderActions=function(warnings){v91Pending=normalizeAll(v91Pending);return originalRender(warnings)};
  applyV9AiActions=function(){if(!v91Pending.length)return;v91Undo={inputs:getInputState(),params:JSON.parse(JSON.stringify(params))};try{const actions=normalizeAll(v91Pending),allowed=v91Snapshot().allowedActions;for(const x of actions){if(!x.field)throw Error('AI не указал изменяемое поле');if(x.value_text===undefined||x.value_text===null||String(x.value_text).trim()==='')throw Error('AI не указал новое значение для поля '+x.field);if(x.type==='set_input'){if(!allowed.set_input.includes(x.field))throw Error('Недопустимое поле: '+x.field);const e=document.getElementById(x.field);if(!e)throw Error('Поле не найдено: '+x.field);e.value=v91Parse(normalizeValue(x.value_text,x.field),x.field)}else if(x.type==='set_param'){const idx=Number(x.target);if(!Number.isInteger(idx)||!params[idx])throw Error('Строка расхода не найдена: '+String(first(x.expense,x.target,x.name)||'не указана'));if(!allowed.set_param.includes(x.field))throw Error('Недопустимое поле расхода: '+x.field);params[idx][x.field]=v91Parse(normalizeValue(x.value_text,x.field),x.field)}else throw Error('Недопустимое действие: '+x.type)}renderParams();syncRanges();recalc();v91Pending=[];v91RenderActions([]);v91Append('Изменения применены и расчёт обновлён.')}catch(e){undoV9AiChange();v91Append('Изменения не применены: '+e.message)}};
  window.__v92AiActionFixV2='${VERSION}';
 })();`;
 const s=d.createElement('script');s.textContent=code;d.body.appendChild(s);d.documentElement.dataset.v92AiActionFixV2=VERSION;return true;
}
frame.addEventListener('load',()=>setTimeout(install,1400));
const timer=setInterval(()=>{if(install())clearInterval(timer)},350);
setTimeout(()=>clearInterval(timer),30000);
})();